# 代码审查报告

## 项目概述
- **项目名称**: Portfolio Website (个人作品集网站)
- **技术栈**: FastAPI (后端) + Next.js (前端) + MongoDB (数据库)
- **审查日期**: 2026-03-02

---

## 一、后端代码审查 (FastAPI)

### 1.1 架构与设计

#### 正面
- 采用分层架构 (routers/services/models)，职责分离清晰
- 使用 Pydantic 进行数据验证和序列化
- 采用依赖注入模式进行认证管理
- 生命周期管理正确处理数据库连接

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 中等 | 数据加载层混合了同步和异步API，可能导致性能问题 | `data_loader.py:85-88` |
| 低 | 部分函数缺少类型提示 | 多处 |

### 1.2 安全性

#### 正面
- 使用 JWT 进行认证，Token有过期时间
- 密码使用 SHA-256 + Salt 哈希存储
- 使用 HMAC 进行密码验证比较
- CORS 配置使用环境变量控制
- 生产环境禁用 Swagger UI

#### 问题
| 严重程度 | 问题描述 | 位置 | 建议 |
|---------|---------|------|------|
| **高** | SHA-256 不适合密码哈希，应使用 bcrypt/Argon2 | `auth.py:42-53` | 更换为 bcrypt |
| 中等 | JWT Secret 缺少密钥轮换机制 | `config.py:22` | 实现密钥版本管理 |
| 低 | 缺少请求速率限制 | - | 添加 SlowAPI 中间件 |
| 低 | 密码哈希函数 `generate_password_hash` 暴露在API中 | `auth.py:141-158` | 移至工具脚本 |

### 1.3 数据库与数据访问

#### 正面
- 使用 Motor 进行异步 MongoDB 操作
- 数据库连接生命周期正确管理
- 模型定义清晰，使用 Pydantic

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 中等 | `data_loader.py:85-88` 使用 `asyncio.run()` 在同步函数中，可能阻塞事件循环 | `data_loader.py` |
| 低 | 缺少数据库索引定义 | - |
| 低 | `db_models.py` 中的部分字段与实际存储格式不一致 | `db_models.py:30` |

### 1.4 错误处理

#### 正面
- 统一使用 HTTPException 处理错误
- 认证失败返回标准 401 响应

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 中等 | 部分路由缺少 try-catch，异常可能泄露内部信息 | `skills.py:156-160` |
| 低 | 错误信息直接暴露给客户端，可能包含敏感信息 | 多处 |

### 1.5 API 设计

#### 正面
- RESTful API 设计
- 使用 OpenAPI 规范（文档已禁用生产环境）
- 统一的响应格式

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 低 | `/api/auth/me` 端点没有实际验证 Token | `auth.py:132-138` |
| 低 | 部分端点缺少分页支持 | - |

---

## 二、前端代码审查 (Next.js)

### 2.1 架构与设计

#### 正面
- 使用 Next.js App Router，符合现代 React 实践
- 组件化设计，职责分离清晰
- 使用 TypeScript 提供类型安全
- 服务端组件与客户端组件分离合理

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 低 | 部分组件缺少类型导出 | - |
| 低 | 类型定义分散在多个文件中，有重复 | `types.ts` vs `admin-api.ts` |

### 2.2 安全性

#### 正面
- 使用环境变量管理 API URL
- 客户端 Token 存储使用 localStorage（标准做法）
- API 调用使用 Bearer Token 认证

#### 问题
| 严重程度 | 问题描述 | 位置 | 建议 |
|---------|---------|------|------|
| **高** | XSS 风险：`dangerouslySetInnerHTML` 可能渲染未转义内容 | 检查使用位置 | 使用 DOMPurify 净化 HTML |
| 中等 | localStorage Token 可能受到 XSS 攻击 | `admin-api.ts:13` | 考虑使用 httpOnly cookie |
| 低 | 缺少 Content Security Policy (CSP) | `layout.tsx` | 添加 CSP meta 标签 |

### 2.3 错误处理

#### 正面
- 全局错误边界组件 (`error.tsx`)
- API 调用有错误处理逻辑
- 404 页面 (`not-found.tsx`)

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 中等 | API 错误时直接显示后端原始错误信息 | `admin-api.ts:47-48` |
| 低 | 部分组件缺少错误状态处理 | - |

### 2.4 性能

#### 正面
- 使用 Next.js 图片优化（如使用）
- 服务端组件减少客户端 JS
- 组件懒加载可能已配置

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 低 | 缺少 `Suspense` 边界优化加载状态 | - |
| 低 | API 调用没有缓存配置 | `types.ts:140-161` |

### 2.5 可访问性 (a11y)

#### 正面
- 项目卡片有正确的 `role` 和 `tabIndex`
- 键盘事件处理

#### 问题
| 严重程度 | 问题描述 | 位置 |
|---------|---------|------|
| 中等 | 部分按钮缺少 aria-label | 检查表单组件 |
| 低 | 颜色对比度需要验证 | Tailwind 默认配置 |

---

## 三、部署与配置

### 3.1 Dockerfile

#### 问题
| 严重程度 | 问题描述 | 建议 |
|---------|---------|------|
| 中等 | 使用 Python 3.11-slim 但未锁定具体 patch 版本 | 使用 `python:3.11.6-slim` |
| 低 | 未使用多阶段构建减小镜像大小 | 考虑多阶段构建 |
| 低 | 缺少健康检查 | 添加 `HEALTHCHECK` |

### 3.2 环境变量

#### 问题
| 严重程度 | 问题描述 | 建议 |
|---------|---------|------|
| 高 | `.env` 文件被提交到仓库 | 添加到 `.gitignore` |
| 中等 | 缺少环境变量验证 | 启动时检查必需变量 |

---

## 四、优先级修复建议

### 立即修复 (高优先级)
1. **后端**: 将 SHA-256 密码哈希更换为 bcrypt
2. **后端**: 从 Git 历史和环境文件中删除 `.env`
3. **前端**: 检查并修复 `dangerouslySetInnerHTML` 的 XSS 风险

### 短期修复 (中优先级)
1. 修复 `data_loader.py` 中的异步阻塞问题
2. 添加 API 速率限制
3. 添加数据库索引
4. 完善错误处理，避免信息泄露

### 长期优化 (低优先级)
1. 实现 JWT 密钥轮换
2. 优化 Docker 镜像构建
3. 添加全面的单元测试
4. 添加 CSP 和更多安全头

---

## 五、总结

该项目整体架构良好，采用了现代的技术栈和合理的分层设计。主要关注点在于：

1. **安全性**: 密码哈希算法需要升级，环境变量管理需要加强
2. **性能**: 部分异步代码实现需要优化
3. **健壮性**: 错误处理和边界情况处理可以更加完善

总体而言，这是一个结构清晰、技术栈现代化的个人作品集项目，通过修复上述高优先级问题，可以达到生产环境的质量标准。
